<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="DT">

	<!-- <insert id="insert" parameterType="dadto">
		insert into
		dateTime_table(d_number,r_date,r_time)
		values(#{d_number},#{r_date},#{r_time})
	</insert>

	
	<select id="check" parameterType="dadto" resultType="dadto">
		select * from dateTime_table where d_number=#{d_number} and r_date = #{r_date} and
		r_time = #{r_time}
	</select> -->
	
</mapper>